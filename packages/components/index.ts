/**
 * 导出所有组件
 */
import CuButton from './button';
import CuAvatar from './avatar';
import CuMessage from './message';

export default [
    CuButton,
    CuAvatar,
    CuMessage
];

export { 
    CuButton, 
    CuAvatar,
    CuMessage
};
